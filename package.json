{
  "name": "pkm",
  "description": "Package manager abstraction and configuration generator",
  "license": "MIT",
  "scripts": {
    "compile": "tsc",
    "test": "tsc && tsc spec/*.ts && jasmine"
  },
  "devDependencies": {
    "jasmine": "^2.4.1",
    "typescript": "^1.8.9"
  }
}
